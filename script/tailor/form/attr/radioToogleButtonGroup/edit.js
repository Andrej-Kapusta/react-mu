import React from "react"
import Combobox from 'tailor/form/attr/comboBox/edit'

// because when items are of type comboBox/optionValue, then it should be a combobox.. fuck you!

export default (props) => (<Combobox {...props} />)